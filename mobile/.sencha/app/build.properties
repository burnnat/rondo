# =============================================================================
# This file provides an override point for default variables defined in these
# lower priority files:
#
#   - touch.properties
#   - *.defaults.properties
#   - defaults.properties
#
# To override a property based on build.environment instead add properties to
# one of these higher priority files:
#
#   - production.properties
#   - testing.properties
#   - native.properties
#   - package.properties
#
# IMPORTANT - Sencha Cmd will merge your changes with its own during upgrades.
# To avoid potential merge conflicts avoid making large, sweeping changes to
# this file.
# =============================================================================
app.build.path=mobile
test.file=test-classes.js

build.dir=${workspace.build.dir}/${app.build.path}/${build.environment}
build.out.archive.path=${workspace.build.dir}/${build.archive.name}/${app.build.path}
build.temp.dir=${workspace.build.dir}/temp/${app.build.path}/${build.environment}
